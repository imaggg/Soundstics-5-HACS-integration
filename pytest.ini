[pytest]
asyncio_mode = auto
# Put the repo root on sys.path so `custom_components` is importable under a
# bare `pytest` invocation (CI), not just `python -m pytest` (which adds cwd).
pythonpath = .
